import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['lh3.googleusercontent.com'], // Add any additional domains here
  },
  /* Add other config options here */
};

export default nextConfig;
